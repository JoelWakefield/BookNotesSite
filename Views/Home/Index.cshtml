@model IEnumerable<Book>
@{
    ViewData["Title"] = "Books";
}


<div id="header-bar" class="header navbar navbar-expand-sm navbar-toggleable-sm navbar-light mb-3">
    <h4><a class="open-menu">Read Book Notes</a></h4>
</div>

<div class="nav-bar">
    @foreach(var book in Model)
    {
        <div class="nav-item">
            <img src="@book.ImageSrc"/>
            <h4>@book.Title</h4>
        </div>
        <br/>
    }
</div>

<div class="text-center flex-parent">
    @foreach(var book in Model)
    {
        <div class="flex-child">

            <p>@book.Author</p>
            <p>@book.Rating/10</p>
            <p>@book.Isbn</p>

            <button class="get-book" id="@book.Id">Read Notes</button>
        
            <h2>@book.Title</h2>
        </div>
    }
</div>

<div id="details-box"></div>
